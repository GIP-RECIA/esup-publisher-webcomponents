{
  "name": "esup-publisher-webcomponents",
  "private": true,
  "workspaces": [
    "@gip-recia/esup-publisher-webcomponents-utils",
    "@gip-recia/color-palette-picker",
    "@gip-recia/js-tree",
    "@gip-recia/subject-infos",
    "@gip-recia/subject-search-button",
    "@gip-recia/evaluator"
  ],
  "scripts": {
    "analyze": "yarn workspaces foreach -At run analyze",
    "build": "yarn workspaces foreach -At run build",
    "lint": "yarn workspaces foreach -At run lint",
    "test": "yarn workspaces foreach -At run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0"
  },
  "packageManager": "yarn@4.0.1"
}
